import React from 'react';
import { Box } from '@mui/material';

const Upload = (props) => {
  return (
    <Box paddingX={{ xs: 3, md: 4 }}>
      <h1>Upload page</h1>
    </Box>
  );
};

export default Upload;
